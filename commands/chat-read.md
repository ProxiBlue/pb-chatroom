---
description: Read all messages in a thread in chronological order
argument-hint: "<thread_id>"
---

Read all messages in a thread using the `chat_read_thread` MCP tool.

Uses MCP tool `chat_read_thread` with:
- `thread_id`: the thread to read

Output formatted as a chronological message list showing sender, timestamp, and body for each message.
