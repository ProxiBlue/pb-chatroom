---
description: Send a reply message to an existing thread via the chat_send MCP tool
argument-hint: "<thread_id> -- <body>"
---

Send a message to an existing thread.

Uses MCP tool `chat_send` with:
- `thread_id`: the target thread
- `body`: message content

Example:

```
Use chat_send with thread_id="<thread_id>" and body="<body>"
```
